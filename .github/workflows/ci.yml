name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verification:
    runs-on: ubuntu-latest
    steps:
      - name: Verify code health and quality
        uses: ./.github/actions/verify
