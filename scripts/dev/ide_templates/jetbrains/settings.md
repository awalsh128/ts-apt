# JetBrains settings template (WebStorm / IntelliJ)

Use these workspace-level defaults for this repository.

## Languages and frameworks
- Node interpreter: project default (Node 24+)
- TypeScript: Use workspace version from `node_modules/typescript`
- ESLint: Automatic configuration from project
- Prettier: Run on save (optional, if you use Prettier in your workflow)

## Editor
- Line separator: LF
- Ensure every saved file ends with a newline
- Strip trailing spaces on save

## Inspections
- Enable JavaScript and TypeScript inspections
- Enable Shell script inspections
- Keep warnings for GitHub Actions YAML enabled

## Terminal
- Default shell: bash
- Working directory: project root
